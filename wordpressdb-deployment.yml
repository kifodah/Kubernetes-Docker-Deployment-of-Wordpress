apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-worpress-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wordpress-frontend
  template:
    metadata:
      labels:
        app: wordpress-frontend
    spec:
      containers:
      - name: wordpress
        image: wordpress:latest
        ports:
        - containerPort: 80
